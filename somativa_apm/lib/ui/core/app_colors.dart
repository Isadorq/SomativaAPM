// lib/ui/_core/app_colors.dart

// Arquivo para estilizar as cores do aplicativo

import 'package:flutter/material.dart';

abstract class AppColors {
  // Cor de fundo principal (preto escuro)
  static const Color  backgroundColor = Color(0xFF202123); 
  // Cor principal do app (Laranja)
  static const Color mainColor = Color(0xFFffa559);
  // Cor de fundo mais clara (para cards, etc.)
  static const Color lightBackgroundColor = Color(0xFF343541);
  // Alias para a cor dos cards
  static const Color fundoCards = Color(0xFF343541);
}